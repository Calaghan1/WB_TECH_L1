
```
В чем разница make и new?

```

Ответ:
```
Функции 'new' и 'make' различаются тем, что: 'new' возвращает указатель вместо значения возвращаемое функцией 'make'. 'make' имеет вариадический аргумент 'size'. 'make' выделяет память и инциализирует только объекты типов: slice, map, or chan.
```